
plot1_func <- function(pow_data){
        ## Create the histogram
        hist(pow_data$Global_active_power, main="Global Active Power", xlab = "Global Active Power (kilowatts)", col="red")
        
}
